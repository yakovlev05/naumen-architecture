# Задание

Программа уже умеет выгружать документы в форматы txt и pdf. Но архитектура сделана не очень удачно. 
Нужно исправить архитектуру так, чтобы выполнялся принцип открытой закрытости (и остальные принципы SOLID). 
В частности, чтобы когда в следующий раз появится задача добавить новый формат файла (например docx), 
то это можно было сделать максимально легко с минимумом изменений.

Решение должно сопровождаться комментариями:
- Сколько нужно изменений сделать, чтобы добавить новый формат и описать какие это действия
- (доп. задание) Найти и исправить другие нарушения принципов SOLID
- (доп. задание) Найти и исправить тёмную сторону ООП

# 📄 Console Document Manager

Простое консольное приложение, позволяющее
импортировать и экспортировать текстовые документы.

------------------------------------------------------------------------

## ✨ Возможности

-   📥 создание документа вручную
-   📥 Импорт `.txt` файлов по указанному пути
-   📄 Хранение документов в оперативной памяти
-   📤 Экспорт документов в директорию `{user.home}/lessonSOLID/` в следующих форматах:
    -   **TXT**
    -   **PDF**

------------------------------------------------------------------------

## 🚀 Запуск

``` bash
mvn spring-boot:run
```

------------------------------------------------------------------------

## 🧩 Команды

Команда  |  Описание
---------|----------------------------------------------
`create` |  Создание документа вручную
`import` |  Импортирует `.txt` файл
`list`   |  Показывает список импортированных документов
`export` |  Экспортирует выбранный документ (TXT/PDF)
`exit`   |  Завершает работу
------------------------------------------------------------------------

## 🔧 Как работает

1.  Приложение запускается в консоли и отображает список доступных
    команд.
2.  Команда **`import`** --- загружает `.txt` файл в память.
3.  Команда **`list`** --- выводит список загруженных документов.
4.  Команда **`export`**:
    -   предлагает выбрать документ по номеру;
    -   спрашивает формат экспорта (`txt` или `pdf`);
    -   сохраняет файл локально.

------------------------------------------------------------------------
